# Downstream Benchmark

* datasets/ contains the 30 downstream datasets

* Metadata/ contains their source and the target column details/

* To run downstream benchmark, specify the following in the "Downstream-Benchmark.ipynb":

1. Input file path
2. The target column
3. The type of task; classification or regression
4. The true feature types of all columns in the source file
